trigger: none

pool:
  name: Default

steps:
  - checkout: self

  - script: |
      echo "Sincronizando código (dry-run)"
      rsync -av --dry-run ./ /home/deploy/apps/praxis_CRM/
    displayName: Dry-run rsync
